services:
  tseapy:
    build: .
    image: tseapy:local
    ports:
      - "5000:5000"
    environment:
      TSEAPY_SECRET_KEY: dev-secret
      TSEAPY_DEBUG: "0"
    command: ["tseapy", "--host", "0.0.0.0", "--port", "5000", "--no-debug"]
